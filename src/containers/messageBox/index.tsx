import * as React from "react";
import "./styles.scss";

interface Props {}

function MessageBox(props: Props) {
  return <div className={"messageBoxContainer"}>message box</div>;
}

export default MessageBox;
